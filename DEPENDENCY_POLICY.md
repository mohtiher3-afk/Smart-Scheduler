# Dependency Policy

- Prefer official AndroidX and Jetpack libraries.
- Minimize third-party dependencies to reduce APK size and build complexity.
- All new dependencies must be approved by the core team.
- Use `libs.versions.toml` for all dependency management.
